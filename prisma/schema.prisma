// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id         String   @id
  username   String
  email      String
  picture    String
  created_at DateTime @default(now())
  chats      Chat[]   @relation(name: "ChatToUser")
}

model Chatroom {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  created_at  DateTime @default(now())
  chats       Chat[]   @relation(name: "ChatToChatroom")
}

model Chat {
  id          Int      @id @default(autoincrement())
  message     String
  user_id     String
  user        User     @relation(name: "ChatToUser", fields: [user_id], references: [id])
  chatroom_id Int
  chatroom    Chatroom @relation(name: "ChatToChatroom", fields: [chatroom_id], references: [id])
  created_at  DateTime @default(now())
}
